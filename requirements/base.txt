# Core framework
Django==5.2.16
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-filter==26.1
drf-spectacular==0.30.0
django-cors-headers==4.9.0

# Config / environment
django-environ==0.14.0

# Database
psycopg[binary]==3.3.4

# Cache / background processing
django-redis==7.0.0
celery==5.6.3
redis==8.0.1

# Static & object storage
whitenoise==6.12.0
django-storages==1.14.6
boto3==1.43.50

# Auth: MFA, passkeys, password hashing, OAuth
pyotp==2.9.0
webauthn==3.0.0
argon2-cffi==25.1.0
requests==2.32.3
reportlab==4.2.5  # payoff schedule PDF export
pytesseract==0.3.13  # receipt OCR (apps.receipts)
Pillow==11.0.0  # receipt image preprocessing
pywebpush==2.3.0  # Web Push delivery (apps.notifications)
# M-Pesa PDF statement import (apps.finance.import_mpesa). Held below 0.11.10,
# which requires Pillow>=12.2 and would drag the pinned Pillow — and with it
# receipt OCR and the payoff-schedule PDF export — along for the ride.
pdfplumber==0.11.9
openpyxl==3.1.5  # Excel (.xlsx) bulk import for bills/recurring (apps.finance.import_xlsx)
