[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
addopts = --reuse-db --cov=apps --cov-report=term-missing --cov-report=xml --cov-fail-under=0
testpaths = tests
markers =
    slow: performance characterisation; excluded from the default run with -m "not slow"
